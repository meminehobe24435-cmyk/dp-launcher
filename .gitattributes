# Normalise line endings so the repository stays consistent across Windows/macOS/Linux.
* text=auto eol=lf

# Binary assets must never be converted.
*.png binary
*.jpg binary
*.gif binary
*.webp binary
*.woff2 binary
*.jar binary
*.apk binary
